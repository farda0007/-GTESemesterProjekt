@page
@model ÆGTESemesterProjekt.Pages.Wishlist.GetAllWishlistProductModel
@{
}
@if (Model.Wishlist != null)
{
    foreach (var wishlist in Model.Wishlist)
    {
        <tr class="btn-secondary">
            <td>
                @wishlist.Product
            </td>
        </tr>
    }
}